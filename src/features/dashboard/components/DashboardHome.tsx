import React from "react";

// Landing view after login. Links into cv/, jobs/, interview-prep/ —
// does not reimplement their logic here.
export function DashboardHome() {
  return (
    <div>
      <h1>Dashboard</h1>
      {/* TODO: links/cards into cv, jobs, matches, interview-prep */}
    </div>
  );
}

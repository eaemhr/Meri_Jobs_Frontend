import React from "react";
import { SessionStart } from "@/features/interview-prep/components/SessionStart";

export default function InterviewPrepPage() {
  return <SessionStart onStart={() => {}} />;
}
